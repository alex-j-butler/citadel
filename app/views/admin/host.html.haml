- content_for(:title) { 'Grant host permissions' }
.row
  %h2.outside Grant tournament host permissions

.panel.panel-container
  .row
    .panel.panel-default
      .panel-body
        = bootstrap_form_tag url: '/admin/host_grant' do |f|
          = f.text_field :user_id
          = f.submit 'Grant'